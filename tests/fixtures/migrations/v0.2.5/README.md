# NuSelf 0.2.5 migration fixture

This is a frozen copy of the complete official example configuration and
representative private records emitted by the `v0.2.5` contracts before the
0.3 runtime and persistence refactor.
Fields introduced in 0.3, including notification runtime context and
per-adapter delivery state, are intentionally absent.
