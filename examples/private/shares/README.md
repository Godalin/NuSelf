# Share Bundles

Share bundles are curated subsets of memory that can be exported or copied for another NuSelf instance.

Do not place private, unreviewed exports in this example directory. Real share bundles should be created under the ignored root `private/shares/` directory.
